cd to the head folder of git
git init (not sure you need this, but it will not do negative things...)
git status
git add *
git status
git commit -m "multi inputs in matrix shape"
git status
git push

